<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : '用户登录 - 电影网站' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<!-- 1. 修改 body：设为 flex 容器，最小高度 100vh，黑色背景 -->
<body class="d-flex flex-column min-vh-100 bg-black text-white"> 
    
    <%- include('../partials/header') %>
    
    <!-- 2. 修改 main：flex-grow-1 占满剩余空间，align-items-center 实现垂直居中 -->
    <main class="flex-grow-1 d-flex align-items-center py-5">
        <div class="container">
            <!-- 3. row justify-content-center 实现水平居中 -->
            <div class="row justify-content-center">
                <!-- 4. 调整宽度：使用 col-md-6 col-lg-4，去掉原本的 col-lg-8 -->
                <div class="col-md-6 col-lg-5 col-xl-4">
                    
                    <!-- 卡片样式优化：添加圆角、阴影 -->
                    <div class="card bg-dark text-white border-secondary shadow-lg rounded-4">
                        <div class="card-body p-4 p-md-5">
                            
                            <div class="text-center mb-4">
                                <h2 class="fw-bold">
                                    <i class="bi bi-box-arrow-in-right text-primary"></i> 用户登录
                                </h2>
                            </div>
                            
                            <!-- 错误提示 -->
                            <% if (typeof error !== 'undefined' && error) { %>
                                <div class="alert alert-danger d-flex align-items-center" role="alert">
                                    <i class="bi bi-exclamation-circle-fill me-2"></i>
                                    <div><%= error %></div>
                                </div>
                            <% } %>

                            <form id="loginForm" action="/auth/login" method="POST">
                                <div class="mb-3">
                                    <label for="username" class="form-label text-secondary">账号</label>
                                    <!-- 输入框样式微调 -->
                                    <div class="input-group">
                                        <span class="input-group-text bg-secondary border-secondary text-white"><i class="bi bi-person"></i></span>
                                        <input type="text" class="form-control bg-dark border-secondary text-white" 
                                            id="username" name="username"
                                            value="<%= typeof formData !== 'undefined' && formData.username ? formData.username : '' %>"
                                            required placeholder="请输入账号">
                                    </div>
                                    <div class="invalid-feedback" id="usernameError"></div>
                                </div>

                                <div class="mb-3">
                                    <label for="password" class="form-label text-secondary">密码</label>
                                    <div class="input-group">
                                        <span class="input-group-text bg-secondary border-secondary text-white"><i class="bi bi-lock"></i></span>
                                        <input type="password" class="form-control bg-dark border-secondary text-white" 
                                            id="password" name="password" required placeholder="请输入密码">
                                    </div>
                                    <div class="invalid-feedback" id="passwordError"></div>
                                </div>

                                <div class="mb-3 form-check">
                                    <input type="checkbox" class="form-check-input bg-dark border-secondary" id="rememberMe" name="rememberMe">
                                    <label class="form-check-label text-secondary" for="rememberMe">
                                        记住我
                                    </label>
                                </div>

                                <button type="submit" class="btn btn-primary w-100 btn-lg mb-3 fw-bold">
                                    登录
                                </button>
                            </form>

                            <div class="text-center mt-3">
                                <p class="mb-0 text-secondary">还没有账号？ 
                                    <a href="/auth/register" class="text-primary text-decoration-none fw-bold">立即注册</a>
                                </p>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </main>
    
    <%- include('../partials/footer') %>
    
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>